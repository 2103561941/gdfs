syntax = "proto3";
option go_package = "datanode/pb";

service DataNode {
}

message Requset {
    string localFilePath = 1;
    string remoteFilePath = 2; 
    bytes databytes = 3;  // file data
}

message Response {
    int64 ack = 1;  // result status
    string msg = 2; // result details
    bytes Databytes = 3;  // file data
}